{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://agent-flywheel.dev/schemas/inputs/flywheel_confirm_impl_models.json",
  "title": "flywheel: flywheel_confirm_impl_models input",
  "description": "One-time gate: show default implement models (flywheel.config.yaml implement:) and persist the user choice before spawning Cursor Task impl agents. Call without confirmImplModels to get implModelsGate; call again with confirmImplModels after the user replies.",
  "type": "object",
  "properties": {
    "cwd": {
      "type": "string",
      "description": "Project working directory (absolute path)"
    },
    "confirmImplModels": {
      "description": "User choice: \"defaults\", { uniform: \"<slug>\" }, or { simple, medium, complex }."
    }
  },
  "required": [
    "cwd"
  ]
}
