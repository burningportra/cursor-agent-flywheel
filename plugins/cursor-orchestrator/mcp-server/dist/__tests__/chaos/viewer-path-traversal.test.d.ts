/**
 * Chaos: GET /assets/../../../../etc/passwd must return 403, never the file.
 * Uses raw HTTP (not URL parsing) so the dotdot segments survive transit.
 */
export {};
//# sourceMappingURL=viewer-path-traversal.test.d.ts.map