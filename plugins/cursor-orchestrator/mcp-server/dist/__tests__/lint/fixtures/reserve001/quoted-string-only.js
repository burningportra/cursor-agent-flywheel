// Fixture: literal "file_reservation_paths" appears but not inside an
// agentMailRPC call — e.g. as documentation. Must NOT trigger RESERVE001.
export const DOC = "Use file_reservation_paths(project_key, agent_name, ...) to reserve files.";
//# sourceMappingURL=quoted-string-only.js.map