node_modules/
# mcp-server/dist/ is committed so the plugin works without a local build; rebuild after src changes.
.pi-orchestrator/checkpoint.json.tmp
.pi-orchestrator/checkpoint.json.corrupt
*.log
.DS_Store
/.beads
/.pi-orchestrator
