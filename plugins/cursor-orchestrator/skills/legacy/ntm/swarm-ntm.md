# NTM swarm appendix (opt-in)

Load when `FW_IMPL_BACKEND=ntm`. Canonical Cursor path: `commands/flywheel-swarm.md`.

Invoke `/vibing-with-ntm`, resolve `NTM_PROJECT`, spawn via `ntm spawn` + `ntm --robot-send` with `program: "claude-code"` per pane. See upstream agent-flywheel-plugin history for the full pane ladder.
