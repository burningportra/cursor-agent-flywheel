# NTM implement appendix

Set `FW_IMPL_BACKEND=ntm`. Use `skills/start/_implement.md` from disk (full profile) or upstream plugin — includes NTM readiness gate, tender-daemon, and `ntm spawn` dispatch.
